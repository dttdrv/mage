# Mage + MageVK — changes and findings, 2026-08-01

Everything changed in the Mage stack on 2026-08-01, and every error and
issue found along the way. Part 1 covers the Wine/Mage launch and windowing
stack on macOS 26/27. Part 2 is the DOOM: The Dark Ages MageVK/SPIRV-Cross
record — every gate the game presented, what was changed to clear it, and
what is still broken. Part 3 lists the currently open issues. All changes
are in the mage tree (mage/, mage/magevk, toolchains/wine-mage-11.13);
upstream MoltenVK and the main Wine work were not touched.

# Live state (2026-08-02)

- magevk branch mage-rt-rayquery: local merge commit e87c07fd contains
  the 4 RT commits d5f35ba5..25440e4e (from the main-project ray-as-minimal
  line) on top of all Mage hooks. Unpushed; origin/mage-rt-rayquery is
  still 96a02ddc.
- mage/sources/SPIRV-Cross-ray: b476f1dd ports the native traceRay fast
  path from upstream/SPIRV-Cross-ray-native-fast-path (which was dirty
  and uncommitted there). One-shot intersector.intersect() when the
  effective traversal flags are exactly Opaque|SkipAABBs, query loop
  otherwise; guarded off under the position-fetch ABI because the unified
  hit/miss tail cannot reference spvRayQuery outside the query-path
  scope. The 0.201 ms figure from that branch was a local traversal
  microbenchmark, not TDA frame time.
- Deployed binary (both mage/dist/runtime-ray-icb/lib and the wine-mage
  toolchain lib, md5 7d8dfb73…): magevk e87c07fd + SPIRV-Cross b476f1dd,
  built in mage/build/MoltenVK-rt-rayquery; Mage hook strings verified
  post-deploy (MAGE_VK_DEVICE_NAME x2). Pre-deploy backups sit next to
  each live dylib as libMoltenVK*.pre-native-fastpath-20260802.
- TDA test 2026-08-02 ~11:10 on this build: launcher GPU gate passed and
  idTechLauncher handed off to DOOMTheDarkAges.exe — the no-handoff seen
  in the merge-only test earlier the same day did NOT reproduce (that
  failure is therefore not attributable to the merge alone; cause
  unknown, possibly a transient launcher/Steam state). The game exe had
  the deployed dylib open (lsof path match). 0 mvk-error, no
  device-lost. MVK performance tracking (300-frame windows): avg
  34.9-36.7 FPS, frame interval avg 27.2-28.6 ms with spikes to 839 ms —
  real frames being submitted in-game. Visual state (flicker, blocky 3D
  corruption, alt-tab-to-progress) was not re-verified in this run; the
  Part 2 §9 suspects stand.

# Part 1 — Launch stack (Wine / Mage / macOS)

## 1. Process-presence gating (Dock/Dock icon/alt-tab)

Goal: exactly one foreground macOS app per game (plus at most one "Steam"
UI). Everything else (wine plumbing, steam helpers, crash reporters) must
never promote to a foreground app.

History in one paragraph: per-step `MAGE_APP_EXE` allowlist → game
re-spawned by Steam missed the list → headless (no Dock, no alt-tab,
window stuck on top at window-layer 26+, uncloseable except by kill).
Recipe-wide union fixed that, but the model still failed CLOSED: any
missing exe entry = headless game. It bit twice in one day (doom2016
`app_exes` dropped in a recipe edit; TDA via a stale /Applications build).
2026-08-01 late: replaced with a **denylist**.

Current mechanism (wine `dlls/ntdll/unix/loader.c`, `loader_exec`):
- `MAGE_BACKGROUND_EXE` — `:`-separated, case-insensitive exe-basename
  denylist, matched on `argv[2]`. Listed exes get `MAGE_BACKGROUND=1`.
- `winemac.drv/cocoa_app.m transformProcessToForeground:` returns early
  when `MAGE_BACKGROUND` is set → process keeps windows but never becomes
  a foreground app.
- Everything else defaults to **foreground**, and the loader derives the
  app name from the exe basename (`DOOMTheDarkAges.exe` → sibling loader
  `DOOMTheDarkAges` → Dock/menu bar show that name; sibling loaders are
  loader copies with a patched embedded Info.plist, made by
  `mage/tools/wine-app-name/patch-wine-loader-name.py`,
  `bin/mage ensure_app_launcher`).
- `MAGE_APP_EXE` strict allowlist mode still honored if set (debug only).
- `MAGE_DEBUG_LOADER=1` → `mage-loader: argv[2]=... allowlist=... denylist=...`
  on stderr for every process. This is the fastest way to see what Steam
  actually spawns.

Denylist contents: steam.exe, steamservice, steamerrorreporter(64),
steamsysinfo, hardwareupdater, *driverquery*, steam_monitor,
steambootstrapper, streaming_client, gameoverlayui, conhost, explorer,
services, svchost, rpcss, plugplay, winedevice, wineboot, rundll32,
msiexec, regsvr32, tabtip, winemenubuilder, winecfg, control, notepad.

Critical: **steamwebhelper_real.exe must stay foreground.** In the
single-process CEF wrapper configuration, that process IS the Steam UI.
When it was background, winemac never promoted it and Steam's watchdog
fired "steamwebhelper is not responding", which also silently killed
`steam.exe -applaunch`. MageCore shows it as "Steam" (display-name
sibling loader); steam.exe itself is a windowless orchestrator on the
denylist.

Verified 2026-08-01: Doom 2016 via applaunch → window owner "DOOMx64vk",
60fps at menu. TDA → window owner "DOOMTheDarkAges", normal layer,
alt-tab in/out works.

## 2. applaunch vs direct exe launch

Doom 2016 direct (`wine DOOMx64vk.exe`) dies at `SteamAPI_Init` → game
prints `FATAL ERROR: Steam failed to initialize` and throws a C++
exception (`e06d7363`). This happens even with `SteamAppId`/`SteamGameId`
env set and CWD = exe dir: Steam injects its full client environment only
into children it launches itself. Conclusion: for Steam games, launch via
`steam.exe -applaunch <appid> [game args...]` (what CrossOver does).
Direct launch remains fine for non-Steam exes (TDA's idTechLauncher works
because it self-handoffs to DOOMTheDarkAges.exe).

Also: DOOM 2016's Vulkan exe is `DOOMx64vk.exe`; `DOOMx64.exe` is the GL
exe. Steam's applaunch for 379720 spawns both processes (one exits); both
are in the recipe's app_exes so both get named loaders.

## 3. Known open issue: TDA flicker / loading stall

Symptoms (2026-08-01): window correct (owner/layer/focus all fine), but
the game sits black at loading until the user alt-tabs to it, then
flickers heavily on interaction; after a second alt-tab the window goes
black. Two independent suspect groups:

a) GPU-side corruption (Part 2 §5, §9): menu UI renders pixel-perfect
   while the 3D scene corrupts into spreading blocky garbage. Suspects:
   private sparse buffers (mandatory — game dies at vkCreateBuffer
   without them, so no A/B), BLAS `nativeSize` growth →
   `retainCurrentGeneration` nullptr.
b) Presentation/focus: foreground promotion enables winemac's
   display-capture fullscreen path (`cocoa_app.m` line ~895,
   `CGCaptureAllDisplays`) — now bypassed by MAGE_FAKE_DISPLAY_MODES
   (§7). The alt-tab-gated loading is the macOS activation-policy issue
   (§7). Second-alt-tab black screen looks like present/drawable
   reacquisition — untested.

MoltenVK perf logging needs BOTH `MVK_CONFIG_PERFORMANCE_TRACKING=1` and
`MVK_CONFIG_PERFORMANCE_LOGGING=1`
(`MVK_CONFIG_PERFORMANCE_LOGGING_FRAME_COUNT=300`); logging alone prints
nothing (TDA recipe has all three).

## 4. Proton-route blocker (for D3D games)

Steam overlay / any D3D path currently loads prefix DXVK, which rejects
the magevk device: `VK_EXT_robustness2`'s `nullDescriptor` feature is
missing → "Failed to initialize DXVK". Recipes must not set `d3d11=n;
dxgi=n` blindly (that was another silent-kill landmine for Doom 2016).
nullDescriptor on magevk is the gate for the whole Proton route.

## 5. Process detection (ps shows nothing)

Wine PE processes clobber argv/environ on macOS 26+: `ps eww` shows a
Windows-style command line and NO environment. Do not match on WINEPREFIX
from ps. Pin processes to a prefix via open files (`lsof +D`-style probe)
— `bin/mage stop` and `MageCore.refreshRunning` both do this. The GUI's
running-state needles must include each launch step's `appExes`, not just
`program`, or a launcher→game handoff drops the running state when the
launcher exits (fixed 2026-08-01, MageCore.swift).

## 6. Housekeeping rule (user-enforced, hard)

After EVERY test run: kill steam/wine/game processes and verify
`ps aux | grep -iE 'wine|steam|doom|idtech' | grep -v grep | wc -l` == 0.
Steam idle processes otherwise accumulate (20+ at one point) and later
runs inherit a dirty wineserver.

## 7. Focus/activation on macOS 26+ (2026-08-01 evening)

Symptom chain being fixed: TDA launched from Mage stayed behind the
terminal, idled at the loading screen (idTech gates rendering on focus),
then flickered after a manual alt-tab.

Fixed and verified:
- `MAGE_FAKE_DISPLAY_MODES=1` (winemac `setMode:forDisplay:` no-op): game
  never captures/mode-switches the physical display. TDA now runs a
  1512x982 fullscreen window, compositor-scaled, no black shield.
- winemac forces activation on a foreground app's first visible window
  (`orderBelow:orAbove:activate:`, gated by the denylist; debug via
  `MAGE_DEBUG_WINEMAC=1`).

Verified dead ends on macOS 26/27 beta (all return success, no effect):
- `[NSApp activate]` and `NSRunningApplication activateWithOptions:` from
  the wine process itself (`MAGE_FORCE_ACTIVATION`, incl. 1s retry).
- System Events `set frontmost of process` from osascript.
- `tell application "X" to activate`.
- Synthetic mouse/keyboard from the computer-use MCP (screenshots work,
  events don't land — permission/OS-beta limitation).

Current mechanism (needs real-user verification): Mage.app issues the
activation itself, gesture-proximate to the Play click —
`MageCore.frontGameProcess(named:)` polls NSRunningApplication for the
game's Dock name (recipe launch-step programs + appExes, minus steam.exe)
and calls `activate(options:[.activateAllWindows,.activateIgnoringOtherApps])`,
with one re-front 15s later for late-created windows. Hooked into
runCLI("run") and openInSteam(steam://run/).

If that also proves insufficient, the next lever is the CrossOver-style
one: per-game minimal .app wrappers launched via NSWorkspace.open, so the
game's process tree originates from a LaunchServices user-driven launch.

# Part 2 — DOOM: The Dark Ages, MoltenVK / SPIRV-Cross saga

First captured 2026-07-29, first verbose launch via
`bin/mage run doom-the-dark-ages` (WINEDEBUG=err+all,+vulkan,
MVK_CONFIG_LOG_LEVEL=4). Raw log: $TMPDIR/mage-doom-the-dark-ages.log.

## 1. Fatal: game aborts on GPU vendor, then NULL write crash (2026-07-29)

Sequence in log: vkCreateInstance OK (MoltenVK 1.4.2, Vulkan 1.4.357) ->
physical device enumeration OK -> game shows "no GPU vendor recognized" ->
`wine: Unhandled page fault on write access to 0x0 at 0x1409EB7F2 (thread 056c)`.

id Tech 8 maps VkPhysicalDeviceProperties.vendorID to an internal vendor enum
(NVIDIA 0x10DE, AMD 0x1002, Intel 0x8086). MoltenVK reports Apple (0x106B),
which hits no table entry; the game then writes through the NULL entry and dies.
This is worked around Wine-side (MAGE_VK_* property spoof in winevulkan), but
the underlying "Apple is not a recognized vendor" problem applies to every
MoltenVK title with a hardcoded vendor table.

## 2. Hard blocker: ray tracing extensions absent (2026-07-29)

id Tech 8 requires hardware ray tracing at all quality levels. Missing from
the host device extension list at the time:

- VK_KHR_ray_tracing_pipeline
- VK_KHR_ray_query
- VK_KHR_acceleration_structure

(VK_KHR_deferred_host_operations was already present.) Resolved since —
see §6.

## 3. winevulkan filters extensions MoltenVK exposes (2026-07-29)

Host (MoltenVK) list contains these, but winevulkan reports them
"not supported" and hides them from the game:

- VK_KHR_portability_subset - REQUIRED by spec for MoltenVK devices; games that
  enable it on principle will fail device creation. Wine's vulkan spec copy
  needs this extension wired through.
- VK_EXT_metal_objects - low impact, rarely requested by games.
- VK_GOOGLE_display_timing - low impact.
- VK_MVK_moltenvk (instance) - low impact; some engines use it to detect
  MoltenVK and pick code paths.

Also absent (noted for future titles): VK_EXT_mesh_shader,
VK_KHR_fragment_shading_rate.

## 4. Staged RT validation + subgroup-stage gate (2026-07-29/31)

The unchanged staged probe identified MoltenVK 1.4.3, exposed all four required
RT extensions, created the device, and resolved 23/23 required procedures. The
game log then exposed `VK_KHR_acceleration_structure`,
`VK_KHR_deferred_host_operations`, `VK_KHR_ray_query`, and
`VK_KHR_ray_tracing_pipeline` to `DOOMTheDarkAges`, so the old
`No ray tracing hardware GPU found` gate did not recur.

The game then deliberately aborted before `vkCreateDevice`. Steam recorded
`DOOMTheDarkAges.exe`, followed six seconds later by `BsSndRpt64.exe`. The
resulting `TitanSteam7CBT5OV6.dmp` contains:

```text
FATAL ERROR: Please update your driver: Could not find support for required subgroup stages
```

The exception is the game's forced null-write fatal path. Disassembly shows
that it requires vertex, fragment, and compute subgroup stages:

```text
(supportedStages & 0x31) == 0x31
```

The failed staged runtime reported `0x32`, so vertex was the only missing stage.

Root cause: the vertex-subgroup support never lived in the 2771/eefe7375
line. It exists only as (a) a 2-file change in the ray-icb experiment tree
(vertex bit in `populateSubgroupProperties`, fixed subgroup size for the
plain vertex stage) and (b) 12 uncommitted lines in
`sources/SPIRV-Cross-ray` on top of 62db8c83. The dirty rt-minimal import
had also re-pinned SPIRV-Cross to upstream 6c09849f, which cannot emit
vertex subgroups either.

Fix ported into Mage-owned trees (all local, main project untouched):

- magevk `mage-rt-minimal` @ 68cc22df: vertex stage bit (Apple GPU +
  simdPermute + simdReduction) and vertex `fixed_subgroup_size`, SPIRV-Cross
  pin restored to 62db8c83.
- SPIRV-Cross `mage-vertex-subgroups` @ a2713ce7 (pushed to mage/spirv-cross):
  the 12-line vertex subgroup emission change, committed with its test
  shaders. Build uses `-DCPM_SPIRV-Cross_SOURCE=mage/sources/SPIRV-Cross-ray`.

Isolated validation: the required basic, vote, arithmetic, ballot, shuffle,
and shuffle-relative operations compile in a Metal 2.3 vertex function on
Apple GPU hardware. The Windows probe moved from
`SUBGROUP stages=0x32 operations=0x6ff size=32` to `stages=0x33`, and a
SPIR-V vertex shader exercising every operation class passed pipeline
creation, one draw, submission, and GPU completion through the full stack.

## 5. Feature gates: three remaining + fp64 + AS compaction (2026-07-31/08-01)

With the vertex-subgroup build deployed, the game passes vendor spoof, RT
extension exposure, and the subgroup-stage check. vkCreateDevice then failed
on exactly three requested features (verified by dumping the full
requested/available feature arrays at the error site):

- sparseBinding
- sparseResidencyBuffer
- shaderBufferInt64Atomics

Everything else the game wants is covered: geometryShader and
shaderCullDistance via the Mage fake flags, wideLines via a
`MVK_USE_METAL_PRIVATE_API=ON` build (now the runtime default), and
shaderResourceMinLod natively (it was never missing; earlier suspicion was
a field-counting error during diagnosis, not a MoltenVK defect).

Notes for whoever implements the rest:

- shaderBufferInt64Atomics cannot be exposed honestly: Metal 4 on the
  M5 Pro does not compile `atomic_ulong` device ops (verified with a
  runtime-compiled MSL probe). It needs emulation in
  MoltenVK/SPIRV-Cross or a game-level workaround. (Emulated 64-bit
  shader atomics have since been added to magevk — see magevk history.)
- sparseBinding/sparseResidencyBuffer: the 554-line vkQueueBindSparse
  emulation experiment in `mage/sources/MoltenVK-ray-icb` was a start but
  incomplete. Faking the feature bits without working sparse backing will
  crash the game at the first sparse operation. Private sparse buffers
  (MAGE_MVK_ENABLE_PRIVATE_SPARSE_BUFFERS=1) are what the live runtime
  uses — mandatory: the game aborts at vkCreateBuffer without them, so no
  A/B against them is possible. They remain a prime corruption suspect.

fp64 in one compute shader (fixed via demotion): one pipeline failed to
compile — `'double' is not supported in Metal` (a single averaging loop
accumulating into a double). SPIRV-Cross-ray `spirv_msl.cpp` demotes fp64
to fp32 in MSL output. Does not cover doubles in buffer blocks; none
encountered so far.

AS compaction copy failures (611x per session, fixed):
`vkCmdCopyAccelerationStructureKHR(): destination has insufficient
capacity` — idTech compacts a BLAS, allocates a destination of exactly
the compacted size, then CLONE-copies the source into it; the clone needs
the source's native (uncompacted) Metal size (~1.56x). Fix in
`MVKAccelerationStructure::retainFullWriteGeneration`: (a) native
capacity grows instead of failing — storage beyond the Vulkan AS size is
privately allocated; (b) `_metadataCapacity` likewise grows. Verified:
zero capacity errors afterwards.

## 6. Launcher GPU gate fully mapped and passed (2026-07-31 → 08-01)

idTechLauncher ("Raytracing Incompatible GPU" dialog) gates on Vulkan data
only. Its full probe: vkCreateInstance + device extension enumeration +
vkGetPhysicalDeviceProperties x2, then vkDestroyInstance and the dialog.
No DXGI, no registry GPU reads, no feature queries. Blockers found and
fixed, in order:

1. winevulkan loader returned NULL from vkGetDeviceProcAddr for device
   functions whose extension is not enabled on that device. Real drivers
   return non-NULL thunks regardless. The game resolves the RT procs on an
   8-extension probe device, caches them in .data globals, and later calls
   through NULL during streaming (both minidumps: call [rip+0x3cc1e70] at
   exe RVA 0xc84112, 0xc0000005). Fixed in wiage: loader.c falls back to
   wine_vk_get_device_proc_addr with a WARN. 329 device functions resolved
   for the game on the next boot, including the full RT set.
2. wine was loading a stale Jul-28 libMoltenVK: install/lib/libMoltenVK.dylib
   was never updated when dist/runtime-ray-icb was, and
   install/lib/libMoltenVK.1.dylib was a real file (1.4.2) shadowing the
   canonical install name. Both now point at the current build. Deploy
   MoltenVK to BOTH dist/runtime-ray-icb/lib and the wine install lib.
3. VkPhysicalDeviceProperties gate fields: vendorID/deviceID/deviceName/
   driverVersion spoofed via MAGE_VK_* (magevk, post-init so Metal feature
   detection stays Apple); deviceType must be DISCRETE (dedicated-VRAM
   check), set via MAGE_VK_DEVICE_TYPE=2; sparseBinding and
   sparseResidency* features + sparseProperties reported with
   MAGE_MVK_ENABLE_PRIVATE_SPARSE_BUFFERS=1.
4. wine itself replaced deviceName: win32u get_physical_device_properties2
   overwrites it from the display-device list ("Apple M5 Pro"), discarding
   the MoltenVK spoof. win32u/vulkan.c now honors MAGE_VK_DEVICE_NAME.
   This was the last gate: with it the launcher saw vendor 0x1002,
   device 0x744C, discrete, driver 0x800156, "AMD Radeon RX 7900 XTX",
   sparse residency reported.
5. Final, non-obvious condition (2026-08-01): after checking the 5 required
   device extensions (deferred_host_operations, pipeline_library,
   ray_tracing_pipeline, acceleration_structure, ray_query), the launcher
   calls vkGetPhysicalDeviceFormatProperties2 for
   VK_FORMAT_R16G16B16A16_UNORM (91) and requires bufferFeatures bit 29 —
   VK_FORMAT_FEATURE_2_ACCELERATION_STRUCTURE_VERTEX_BUFFER_BIT_KHR.
   Windows drivers set that bit on the UNORM formats; magevk only mapped
   the six spec-legal AS vertex formats, so the bit read 0 and the
   launcher bailed. Fix: magevk commit 54f381b8 (branch mage-rt-rayquery)
   maps R16G16_UNORM and R16G16B16A16_UNORM to
   MTLAttributeFormatUShort{2,4}Normalized.

With that, the launcher passes the gate, spawns DOOMTheDarkAges.exe, and
the game creates a device with acceleration_structure + ray_query +
ray_tracing_pipeline enabled, submits work, and presents frames
(device-creation excerpt: Appendix E1). One
non-fatal warn: the game asks for
rayTracingPipelineTraceRaysIndirect which magevk does not expose; the game
continues without it.

The launcher also probes D3DKMT: D3DKMTEnumAdapters2 (matches the Vulkan
deviceLUID against KMT adapter LUIDs — wine's are consistent) and
D3DKMTQueryAdapterInfo(KMTQAITYPE_WDDM_2_7_CAPS=70), which wine did not
implement (STATUS_NOT_IMPLEMENTED; gracefully skipped by the launcher,
but now implemented in win32u/d3dkmt.c anyway).

Build gotcha that cost one cycle: overwriting a loaded dylib in place with
cp invalidates its cached code signature (kernel: rejecting invalid page,
cs_mtime != mtime, process killed: 9). Re-sign after in-place deploys:
codesign --sign - --force <dylib>.

## 7. Post-launch crash: null call in TLAS build (fixed 2026-08-01)

After the RT pipeline links, the game builds its first TLAS and calls a
global function pointer that is NULL (0xc0000005 at exe+0xC84112). The
call is vkGetAccelerationStructureBuildSizesKHR, resolved through
vkGetInstanceProcAddr; wine's GIPA returned NULL for any device-level
function. Real drivers/loaders return device procs from GIPA. Fix: wiage
`dlls/winevulkan/loader.c` — when the instance-function check fails, fall
through to `wine_vk_get_device_proc_addr` (WARN + return thunk). One boot
session then logged 1001 GIPA-fallback resolutions.

## 8. Post-launch crash: requiredMinWaveSize / subgroup size control (fixed)

The game killed itself (`mov dword ptr [0], 0x12345678`, idTech fatal
stub): "Could not ensure that compute pipeline ... gets compiled using
the requiredMinWaveSize ... hardware doesn't have support for setting the
required wave size for the compute shader stage". magevk advertised
`requiredSubgroupSizeStages = 0`. Fix: advertise
VK_SHADER_STAGE_ALL_GRAPHICS | COMPUTE. MoltenVK ignores
VkPipelineShaderStageRequiredSubgroupSizeCreateInfo at pipeline creation,
so any requested size is accepted; Metal's thread execution width is 32.

## 9. Slow-boot / white-window phase (diagnosed 2026-08-01, partially historical)

With the crashes fixed the game booted fully (Saved Games config r_mode
21, all RT pipelines built) — then sat on a white window at ~0.3 fps.
Findings then:

- Live render loop confirmed by sampling (AS builds, descriptor updates,
  binds; vkWaitForFences/vkWaitSemaphores 84% of samples).
- vkQueuePresentKHR almost never sampled; the one captured stack sat in
  client_surface_present -> getCAMetalDrawable — blocked waiting for a
  free drawable.
- GPU genuinely busy: Device Utilization 74-78%, 13 GB allocated; no GPU
  recovery events. Not a deadlock — pathologically slow frames with all 3
  swapchain images in flight.

Open hypotheses from that diagnosis (still relevant to the current
flicker/perf complaints):

1. Per-frame full AS rebuilds are pathological in our implementation (the
   game rebuilds/refits every frame; fine on native drivers, seconds here).
2. GPU-side infinite/very long ray-traversal: if a grown generation
   (nativeSize > _size) is later referenced as an instance source,
   retainCurrentGeneration returns nullptr and the TLAS may encode a
   stale/empty AS reference, sending traversal into garbage.
3. A game-side validation/readback loop that our emulation can never
   satisfy.

Later same day the game booted fully again and presented frames; the
user played briefly and reported it "super slow" with FPS dips and
flicker. Correction (2026-08-02): an earlier revision of this section
claimed the game "became genuinely playable (user played the campaign)"
— that was never verified and is retracted. The verified state is only:
the game boots, passes all launcher gates, reaches its menu/loading,
and presents frames; nothing beyond a brief, slow, flickering session
has been user-confirmed. The in-scene corruption (blocky garbage over
3D, UI clean) still stands with the same two suspects (private sparse
buffers, BLAS nativeSize growth / retainCurrentGeneration). Video
evidence (Screen Recording 2026-08-01 at 17.58.08.mov, main menu):
blocky corruption is confined to the 3D backdrop and accumulates across
frames while all UI/text stays clean — consistent with corrupted
geometry/sparse data rather than a present/swapchain fault.

## 10. Fast-path MoltenVK (25440e4e) FAILS the launcher GPU gate (2026-08-01; root cause confirmed 2026-08-02)

GPT's `upstream/MoltenVK-ray-as-minimal/build-ray-native-fast-path` build
(rev 25440e4e "Add ray tracing maintenance1 support", 6.28 MB) makes
idTechLauncher show "GPU Driver Error" (raytracing-incompatible gate) and
the game exe never spawns. The magevk rayquery build (13.35 MB,
dist/runtime-ray-icb) passes the same gate with identical env.

Root cause — CONFIRMED 2026-08-02 (wording corrected after GPT review):
the fast-path build was made from the main-project worktree
`upstream/MoltenVK-ray-as-minimal`, which contains NONE of the magevk
Mage hooks. Confirmed absent from that build: the discrete device type,
the sparse feature bits and sparse implementation, the fake feature
support (geometryShader/shaderCullDistance), and the required UNORM
acceleration-structure format mapping. Whether the launcher literally
saw vendorID 0x106B is NOT proven — Wine spoofs vendor/name
independently of the dylib, even with the upstream build — so the exact
first launcher field that rejected it was never captured. What is valid
is the artifact comparison itself: testing the raw upstream binary
against the magevk build was an invalid comparison, because the two
differ in device identity, sparse feature bits, and feature faking — it
was never ONLY an extension-list delta (the extension lists are
identical; see below). Evidence: Appendix E3 (strings counts: 0
Mage-hook symbols in the fast-path dylib vs 2 in the magevk build; 0
grep matches for the hook env vars in the
upstream/MoltenVK-ray-as-minimal source tree).

Fix — DONE 2026-08-02 (local, unpushed): the 4 RT commits from the
fast-path line (d5f35ba5..25440e4e) are merged into mage/magevk as
local merge commit e87c07fd on branch mage-rt-rayquery, so the fast
path sits on top of the Mage hooks instead of replacing them. Not
pushed: origin/mage-rt-rayquery remains at 96a02ddc. The separate
SPIRV-Cross native-fast-path delta (upstream/SPIRV-Cross-ray-native-fast-path,
branch agent/ray-native-fast-path, dirty/uncommitted as of GPT's
report) is NOT yet integrated into mage/sources/SPIRV-Cross-ray and NOT
application-tested; its 0.201 ms figure is a local traversal
microbenchmark, not TDA frame time. Post-merge TDA test result: see
the live-state section at the top of this document.

Deployment facts (unchanged):

- Instance extension lists are IDENTICAL between the two builds (160
  exts, incl. VK_KHR_ray_tracing_pipeline, ray_query,
  acceleration_structure, ray_tracing_maintenance1,
  deferred_host_operations, descriptor_indexing) — consistent with the
  confirmed root cause: the gate failure is device identity/features,
  not extension exposure.
- Fast-path build is staged at mage/dist/runtime-ray-fastpath/lib
  (picker-visible, not default). Toolchain fallback lib restored to the
  rayquery build; backup kept as
  install-macos12-freetype/lib/libMoltenVK.dylib.rt-rayquery-20260801.bak.
- Also observed: the game binds MoltenVK via DYLD_LIBRARY_PATH from the
  runtime's vulkan_library_path when the wineserver session starts clean,
  but falls back to toolchain lib/libMoltenVK.dylib otherwise — the
  toolchain copy must always be a working build, it is the real default.

# Part 3 — Currently open issues (2026-08-01)

1. TDA loading gate: game sits at loading until a manual alt-tab (macOS
   activation policy, Part 1 §7). Mage-side frontGameProcess shipped,
   needs real-user verdict from a GUI launch.
2. TDA in-scene flicker/corruption (Part 2 §9): private sparse buffers
   (mandatory, no A/B) and BLAS nativeSize growth are the suspects.
3. TDA second-alt-tab black screen: present/drawable reacquisition,
   untested.
4. TDA perf: "super slow" + flicker (user report from a brief session;
   nothing more has been user-confirmed). Fast-path root cause confirmed
   (Part 2 §10); RT commits merged into magevk as local merge e87c07fd
   (unpushed); SPIRV-Cross native-fast-path delta not yet integrated.
5. Proton route for D3D games: nullDescriptor on magevk (Part 1 §4).
6. Mage GUI polish items from user feedback (settings overhaul,
   achievements spinner, game sizes, art loading) — tracked in the repo.

Housekeeping: the TDA recipe no longer sets WINEDEBUG=+vulkan /
MVK_CONFIG_TRACE_VULKAN_CALLS / MVK_CONFIG_SHADER_DUMP_DIR — the +vulkan
trace grew the mage log to 8.5 GB in one session and filled the disk
mid-build.

# Appendix — Evidence

All excerpts verbatim, gathered 2026-08-02. `$TMPDIR` on the test machine
is `/var/folders/hg/qpkk9hrs2zlbtk9nfmw2zztm0000gn/T/`.

## E1. Current TDA log ($TMPDIR/mage-doom-the-dark-ages.log, 2026-08-02, 45778 lines)

GPU spoof active via the winevulkan MAGE_VK_* path (Part 2 §6 gate 3/4):

```text
01f0:fixme:vulkan:mage_spoof_physical_device_properties mage: spoofing GPU identity (vendor 0x1002 device 0x744c driver 0x800156 name 'AMD Radeon RX 7900 XTX')
```

MoltenVK reports the spoofed identity to the game:

```text
	model: AMD Radeon RX 7900 XTX
	vendorID: 0x1002
```

The game's device, created with the full RT feature set (log line 5600) —
this is the "passes the gate, creates the RT device, submits work" claim
of Part 2 §6:

```text
[mvk-info] Created VkDevice to run on GPU AMD Radeon RX 7900 XTX with the following 14 Vulkan extensions enabled:
	VK_KHR_acceleration_structure v13
	VK_KHR_deferred_host_operations v4
	VK_KHR_get_memory_requirements2 v1
	VK_KHR_pipeline_library v1
	VK_KHR_ray_query v1
	VK_KHR_ray_tracing_pipeline v1
	VK_KHR_swapchain v70
	VK_KHR_synchronization2 v1
	VK_EXT_calibrated_timestamps v2
	VK_EXT_hdr_metadata v3
	VK_EXT_memory_budget v1
	VK_EXT_shader_subgroup_ballot v1
	VK_EXT_subgroup_size_control v2
	VK_EXT_swapchain_maintenance1 v1
```

mvk-warn lines, unique with counts (740 total; repeated spam deduped):

```text
 739 [mvk-warn] VK_ERROR_FEATURE_NOT_PRESENT: Blending is enabled for attachment with format VK_FORMAT_E5B9G9R9_UFLOAT_PACK32, which does not support it.
   1 [mvk-warn] VK_ERROR_FEATURE_NOT_PRESENT: vkCreateDevice(): Leaving unavailable physical device feature specified by the 5th flag in VkPhysicalDeviceRayTracingPipelineFeaturesKHR disabled.
```

(The 5th flag in VkPhysicalDeviceRayTracingPipelineFeaturesKHR is
rayTracingPipelineTraceRaysIndirect — the non-fatal warn noted in
Part 2 §6.)

Negative findings in this log (verified absent by grep): no `FATAL ERROR`
lines, no `[mvk-error]` lines, no launcher GPU-gate error lines (this
session passed the gate), no `mage-winemac`/`mage-loader` debug lines
(debug env was not set for this session). Activity level: 32083
`[mvk-info]` lines.

## E2. Older logs still in $TMPDIR

`mage-doom2016.log` (2026-08-01, 731 KB) — loader gating debug
(Part 1 §1; allowlist mode, pre-denylist):

```text
mage-loader: argv[0]=(null) argv[1]=.../i386-unix/wine argv[2]=C:\Program Files (x86)\Steam\steam.exe allowlist=steamwebhelper.exe:steamwebhelper_real.exe:DOOMx64vk.exe:DOOMx64.exe
```

and the Doom 2016 render-pipeline defect seen in that session:

```text
[mvk-error] VK_ERROR_INITIALIZATION_FAILED: Render pipeline compile failed (Error code 2):
Vertex attribute in_TexCoord1(4) is missing from the vertex descriptor.
```

Also present: `mage-collect-doom-the-dark-ages-summary.txt` and
`mage-collect-tda-sparse-off-summary.txt` (2026-08-01) — the latter shows
GPU device util 69-75% (avg 73) and 0 GPU recoveries during a TDA run,
backing Part 2 §9's "GPU genuinely busy, not a deadlock".

## E3. Part 2 §10 — strings proof that the fast-path build lacks the Mage hooks

Commands and output, verbatim (2026-08-02):

```text
$ strings /Users/dttdrv/Projects/macgaming/mage/dist/runtime-ray-fastpath/lib/libMoltenVK.1.4.3.dylib | grep -c MAGE_VK_DEVICE_NAME
0
$ strings /Users/dttdrv/Projects/macgaming/mage/dist/runtime-ray-icb/lib/libMoltenVK.dylib | grep -c MAGE_VK_DEVICE_NAME
2
$ strings /Users/dttdrv/Projects/macgaming/mage/dist/runtime-ray-fastpath/lib/libMoltenVK.1.4.3.dylib | grep -c MAGE_MVK_ENABLE_PRIVATE_SPARSE_BUFFERS
0
$ strings /Users/dttdrv/Projects/macgaming/mage/dist/runtime-ray-icb/lib/libMoltenVK.dylib | grep -c MAGE_MVK_ENABLE_PRIVATE_SPARSE_BUFFERS
2
```

Source-tree confirmation — the fast-path build's worktree contains no
Mage-hook code at all:

```text
$ grep -rl 'MAGE_VK_DEVICE_NAME\|MAGE_MVK_ENABLE_PRIVATE_SPARSE_BUFFERS' /Users/dttdrv/Projects/macgaming/upstream/MoltenVK-ray-as-minimal/MoltenVK/
(no output — 0 matching files)
```

Sanity check, magevk does carry the hooks:

```text
$ grep -rl 'MAGE_VK_DEVICE_NAME' mage/magevk/MoltenVK/
magevk/MoltenVK/MoltenVK/GPUObjects/MVKDevice.mm
```
